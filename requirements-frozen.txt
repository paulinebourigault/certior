# requirements-frozen.txt - pinned dependency snapshot for legacy tooling.
#
# This file is DERIVED from pyproject.toml; it is not the source of truth.
# Prefer ``pip install -e ".[all]"`` (or ``pip install certior[all]`` from
# PyPI) so the project's actual dependency markers and extras are used.
# Regenerate this snapshot when pyproject.toml changes.
#
# Core
z3-solver>=4.12.0
httpx>=0.26.0
pydantic>=2.5.0
jsonschema>=4.21.0
PyYAML>=6.0.2

# API server
fastapi>=0.109.0
uvicorn>=0.27.0
websockets>=12.0

# LLM
anthropic>=0.39.0

# Observability (optional)
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-exporter-otlp>=1.22.0

# Testing (dev only)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
