# Certior Frontend Environment Variables
# Copy to .env.local and adjust as needed.

# Backend API URL (leave blank to use Next.js rewrites via next.config.js)
NEXT_PUBLIC_API_URL=

# Optional explicit WebSocket URL for execution streaming.
# Leave blank to derive from NEXT_PUBLIC_API_URL or local development defaults.
NEXT_PUBLIC_WS_URL=
