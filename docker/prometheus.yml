# Prometheus configuration for Certior
# Used by: docker compose --profile observability up

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "certior"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
    scrape_interval: 10s
